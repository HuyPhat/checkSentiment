import React, {Component} from 'react';

const CustomLoader = (width = '100%', height= '100%') => (
    <img src="/assets/images/loader.gif" height={height} width={width}/>
)

export default CustomLoader;